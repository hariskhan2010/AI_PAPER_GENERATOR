'use client'

import type { PaperData } from '@/types'

interface PaperEditorProps {
  paperData: PaperData
  onChange: (data: PaperData) => void
}

export function PaperEditor({ paperData, onChange }: PaperEditorProps) {
  return null
}
